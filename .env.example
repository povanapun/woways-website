# Copy this file to .env and set your own values before deploying.
# These control the login for the /admin panel — change them from the defaults.

ADMIN_USER=admin
ADMIN_PASSWORD=change-this-password

# Optional: most hosts set this automatically. Only set it if you need a fixed port locally.
# PORT=3000
